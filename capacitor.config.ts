import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'xwill.music.co',
  appName: 'music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
